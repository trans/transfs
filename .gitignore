/lib/
/bin/
/.shards/
*.dwarf
/docs/api/
/docs/*.html
.mcp.json
# the index is a rebuildable cache, never committed
.transfs/index.db
**/.transfs/index.db
# the demo store + mountpoint are generated by demo/seed.sh, not committed
/demo/store/
/demo/mnt/
# the default scratch store (created on demand when no --store is given)
/test/
